import React, { Component } from 'react';

export default class Note extends Component{
  render() {
    // const { note } = this.props;

    return (
      <div className="notes">
        {/* <p>{note[0]}</p> */}
      </div>
    );
  }
}